<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!--
        Initial seed data for role table.
        Author: alona
        Branch: feature/user-service
    -->
    <changeSet id="001-role-data" author="alona">
        <preConditions onFail="HALT">
            <tableExists schemaName="dbo" tableName="role"/>
        </preConditions>

        <sql>
            INSERT INTO dbo.role (name, description) VALUES
                ('ADMIN',    'System administrator with full access'),
                ('CUSTOMER', 'Regular customer who can rent scooters'),
                ('STAFF',    'Service staff responsible for scooter maintenance');
        </sql>

        <rollback>
            <sql>DELETE FROM dbo.role WHERE name IN ('ADMIN', 'CUSTOMER', 'STAFF');</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
